ktor:
  application:
    modules:
      - com.psmo.ApplicationKt.module
  deployment:
    port: 8080

# Active profile (dev or prod)
profiles:
  active: ${KTOR_PROFILE:dev}

# Common configuration
# Profile-specific settings in application-dev.yaml and application-prod.yaml

# CORS configuration - defined in profile-specific files
# Using /api proxy: Same-origin requests, CORS not strictly needed but kept for flexibility
