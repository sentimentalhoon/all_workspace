# =============================================================================
# Production Environment Variables for Docker Compose
# =============================================================================
# Copy this file to .env.prod and update with actual values
# NEVER commit .env.prod to git (it's in .gitignore)

# =============================================================================
# Domain Configuration
# =============================================================================
PSMO_DOMAIN=mycommunity.duckdns.org

# =============================================================================
# SSL/TLS Configuration (Let's Encrypt)
# =============================================================================
# Replace with your actual email for SSL certificate notifications
CERTBOT_EMAIL=your-email@example.com

# =============================================================================
# PSMO Community - PostgreSQL Database
# =============================================================================
PSMO_POSTGRES_DB=psmo_community
PSMO_POSTGRES_USER=psmo
# REQUIRED: Change to a strong password (min 32 chars recommended)
PSMO_POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_4

# =============================================================================
# PSMO Community - Redis Cache
# =============================================================================
# REQUIRED: Change to a strong password
PSMO_REDIS_PASSWORD=CHANGE_ME_STRONG_PASSWORD_5

# =============================================================================
# PSMO Community - MinIO Object Storage
# =============================================================================
PSMO_MINIO_USER=minioadmin
# REQUIRED: Change to a strong password
PSMO_MINIO_PASSWORD=CHANGE_ME_STRONG_PASSWORD_6

# =============================================================================
# PSMO Community - CORS Configuration
# =============================================================================
# Frontend domain for CORS (uses /api path proxy - same origin)
PSMO_CORS_ORIGINS=https://mycommunity.duckdns.org

# =============================================================================
# Telegram Login (Used by both backend & frontend)
# =============================================================================
# REQUIRED: Bot token from @BotFather
TELEGRAM_BOT_TOKEN=CHANGE_ME_TELEGRAM_BOT_TOKEN
# REQUIRED: Numeric bot ID (digits only)
TELEGRAM_BOT_ID=8578829111
# REQUIRED: Bot username without @
TELEGRAM_BOT_USERNAME=Psmo_community_bot

# =============================================================================
# IMPORTANT SECURITY NOTES
# =============================================================================
# 1. CHANGE ALL "CHANGE_ME_*" PASSWORDS before deployment
# 2. Use strong, random passwords (minimum 32 characters)
# 3. Each password should be unique
# 4. Update CERTBOT_EMAIL with your real email address
# 5. Keep .env.prod file secure - only on production server
# 6. Never commit .env.prod to version control
#
# Generate strong passwords:
#   openssl rand -base64 32
#   or
#   pwgen -s 32 1

